name: Pack
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    if: ${{ !contains(github.event.head_commit.message, 'nobuild') }}
    steps:
      - uses: actions/checkout@v4
      - name: Generate Pack
        run: ./pack.sh
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: aabric-a1.1.2_01.zip
          path: ./aaabric-a1.1.2_01.zip
      - name: Create Shorthand Hash
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: aaabric-a1.1.2_01.zip
          name: ${{ steps.vars.outputs.sha_short }} (Generated Release)
          body: |
            Autogenerated release via actions.
            https://github.com/floofyplasma/aabric-prism-instance/releases/download/${{ steps.vars.outputs.sha_short }}/aaabric-a1.1.2_01.zip
          tag_name: ${{ steps.vars.outputs.sha_short }}
